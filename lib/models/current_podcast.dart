class CurrentPodcast {
  String name;
  String episodeTitle;
  String image;
  int podcast_index;
  int audio_index;

  CurrentPodcast({
    this.name, this.image, this.podcast_index, this.audio_index, this.episodeTitle
  });

}