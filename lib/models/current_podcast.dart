class CurrentPodcast {
  String name;
  String episodeTitle;
  String image;
  int podcastIndex;
  int audioIndex;

  CurrentPodcast({
    this.name, this.image, this.podcastIndex, this.audioIndex, this.episodeTitle
  });

}